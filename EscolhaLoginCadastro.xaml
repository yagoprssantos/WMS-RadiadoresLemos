<Window x:Class="WMS_RadiadoresLemos_WPF.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login e Cadastro" 
        Height="700" Width="1000"
        MinHeight="700" MinWidth="1000">

    <!-- Definição de Recursos -->
    <Window.Resources>
        <!-- Definição de Cores -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#003366"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#00509E"/>
        <SolidColorBrush x:Key="AccentColor" Color="#00A4CC"/>
        <SolidColorBrush x:Key="ButtonForegroundColor" Color="White"/>
        <SolidColorBrush x:Key="ButtonBackgroundColor" Color="#00509E"/>
        <SolidColorBrush x:Key="ButtonHoverColor" Color="#003366"/>
        <SolidColorBrush x:Key="ButtonBorderColor" Color="#00A4CC"/>
        <SolidColorBrush x:Key="LightGray" Color="#E0E0E0"/>
        <!-- Cor cinza muito clara -->

        <!-- Estilo para Botões -->
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource ButtonForegroundColor}"/>
            <Setter Property="Background" Value="{StaticResource ButtonBackgroundColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,10"/>
            <!-- Margin ajustada para espaçamento entre botões -->
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5"
                                Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource ButtonHoverColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Layout Principal -->
    <Grid>
        <!-- Border para adicionar bordas arredondadas e cor -->
        <Border Background="White" BorderBrush="{StaticResource LightGray}" BorderThickness="2" CornerRadius="15"
                Padding="20" Width="300" VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- StackPanel centralizado dentro do Border -->
            <StackPanel>
                <!-- Botão de Login -->
                <Button Content="Login" FontSize="16" Width="200" Click="Login_Click"/>

                <!-- Botão de Cadastro -->
                <Button Content="Cadastro" FontSize="16" Width="200" Click="Cadastro_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>