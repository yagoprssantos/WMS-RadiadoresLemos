<UserControl x:Class="WMS_RadiadoresLemos_WPF.DashboardUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Dashboard" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0, 0, 0, 20" Grid.Row="0"/>

        <!-- Painéis para Contadores e Resumos -->
        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Stretch">

            <!-- Contadores (Usuários, Produtos, etc.) -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0, 10, 0, 10">
                <Border Background="#2196F3" Padding="15" CornerRadius="10" Margin="10">
                    <StackPanel>
                        <TextBlock Text="Total de Usuários" FontSize="16" Foreground="White" FontWeight="Bold"/>
                        <TextBlock x:Name="TotalUsuariosTextBlock" FontSize="24" Foreground="White" FontWeight="Bold" Text="0"/>
                    </StackPanel>
                </Border>
                <Border Background="#4CAF50" Padding="15" CornerRadius="10" Margin="10">
                    <StackPanel>
                        <TextBlock Text="Total de Produtos" FontSize="16" Foreground="White" FontWeight="Bold"/>
                        <TextBlock x:Name="TotalProdutosTextBlock" FontSize="24" Foreground="White" FontWeight="Bold" Text="0"/>
                    </StackPanel>
                </Border>
                <Border Background="#FFC107" Padding="15" CornerRadius="10" Margin="10">
                    <StackPanel>
                        <TextBlock Text="Produtos com Baixo Estoque" FontSize="16" Foreground="White" FontWeight="Bold"/>
                        <TextBlock x:Name="ProdutosBaixoEstoqueTextBlock" FontSize="24" Foreground="White" FontWeight="Bold" Text="0"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Gráfico de Barras para Exemplo de Produtos Vendidos -->
            <GroupBox Header="Vendas Recentes" Margin="10">
                <lvc:CartesianChart x:Name="GraficoVendas" Height="300" Margin="10">
                    <lvc:CartesianChart.Series>
                        <lvc:ColumnSeries Title="Produtos Vendidos" Values="{Binding ProdutosVendidosSeries}" Fill="#FF4081"/>
                    </lvc:CartesianChart.Series>
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Dias" Labels="{Binding DiasVendas}"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Quantidade" LabelFormatter="{Binding FormatadorDeEixoY}"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </GroupBox>

            <!-- Logs Recentes -->
            <GroupBox Header="Últimos Eventos" Margin="10">
                <ListBox x:Name="UltimosEventosListBox" Height="150" />
            </GroupBox>

        </StackPanel>
    </Grid>
</UserControl>
