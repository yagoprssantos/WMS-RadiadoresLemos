<UserControl x:Class="WMS_RadiadoresLemos_WPF.UsuariosUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

    <DockPanel Margin="20">

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" DockPanel.Dock="Bottom">
            <!-- Botão discreto para atualizar a DataGrid -->
            <Button Content="🔄" ToolTip="Atualizar" Click="AtualizarDataGrid_Click" 
                    HorizontalAlignment="Left" VerticalAlignment="Center" 
                    Background="Transparent" Foreground="White" 
                    BorderBrush="Transparent" FontSize="25"/>
            <Button Content="Adicionar Usuário" Click="AdicionarUsuario_Click" Width="195" Margin="5" FontSize="18"/>
            <Button Content="Editar Usuário" Click="EditarUsuario_Click" Width="150" Margin="5" FontSize="18"/>
            <Button Content="Deletar Usuário" Click="DeletarUsuario_Click" Width="150" Margin="5" FontSize="18" Style="{StaticResource CancelButtonStyle}"/>
        </StackPanel>

        <Grid DockPanel.Dock="Top" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Pesquisar:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="18" Foreground="White"/>
            <TextBox Name="SearchBox" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Top" FontSize="18" TextChanged="SearchBox_TextChanged"/>
        </Grid>

        <DataGrid x:Name="UsuariosDataGrid"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  Margin="0,0,0,10"
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                  Width="Auto" Height="Auto" FontSize="18">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="E-mail" Binding="{Binding Email}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Permissão" Binding="{Binding Permissao}" IsReadOnly="True" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
