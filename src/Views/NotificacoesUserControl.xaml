<UserControl x:Class="WMS_RadiadoresLemos_WPF.NotificacoesUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="800"
             TextBlock.Foreground="White">

    <Grid>
        <TabControl>
            <!-- Aba de Histórico -->
            <TabItem Header="Histórico" Name="AbaHistorico" FontSize="18">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="20">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Width="500">
                            <TextBlock Text="Histórico do Estoque" FontSize="20" Foreground="White" HorizontalAlignment="Center" Margin="0, 0, 0, 10"/>
                            <DataGrid Name="HistoricoDataGrid"
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      SelectionMode="Single"
                                      SelectionUnit="FullRow"
                                      HorizontalAlignment="Stretch"
                                      VerticalAlignment="Stretch">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Data" Binding="{Binding Data}" Width="*"/>
                                    <DataGridTextColumn Header="Mensagem" Binding="{Binding Mensagem}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <!-- Aba de Alerta -->
            <TabItem Header="Alerta" Name="AbaAlerta" FontSize="18">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="20">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Width="500">
                            <TextBlock Text="Notificações de Alerta" FontSize="20" Foreground="White" HorizontalAlignment="Center" Margin="0, 0, 0, 10"/>
                            <DataGrid Name="AlertaDataGrid"
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      SelectionMode="Single"
                                      SelectionUnit="FullRow"
                                      HorizontalAlignment="Stretch"
                                      VerticalAlignment="Stretch">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Data" Binding="{Binding Data}" Width="*"/>
                                    <DataGridTextColumn Header="Alerta" Binding="{Binding Alerta}" Width="*"/>
                                    <DataGridTextColumn Header="Detalhes" Binding="{Binding Detalhes}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
