<UserControl x:Class="WMS_RadiadoresLemos_WPF.UsuariosUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="630" d:DesignWidth="800">

    <Grid Margin="15">
        <Border BorderBrush="{StaticResource SecondaryBrush}" BorderThickness="1" CornerRadius="15" Padding="25" Background="{StaticResource SecondaryBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Título acima da barra de pesquisa -->
                <Grid Margin="0,0,0,15" Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="1" Background="{StaticResource PanelBackgroundBrush}" CornerRadius="18" Padding="10" Margin="0,0,0,10">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image Width="24" Height="24" Margin="0,0,10,0"/>
                            <TextBlock Text="Gerenciamento de Usuários" Foreground="{StaticResource TextBrush}" FontSize="18" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <Border Grid.Row="1" CornerRadius="15" Background="{StaticResource PanelBackgroundBrush}" Padding="5" Margin="0,0,0,10">
                    <DataGrid Name="UsuariosDataGrid" Style="{StaticResource CustomDataGridStyle}"
                             MouseDoubleClick="UsuariosDataGrid_MouseDoubleClick"
                             Background="Transparent"
                              BorderBrush="Transparent">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" IsReadOnly="True" Width="*" MinWidth="150" MaxWidth="500"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" IsReadOnly="True" Width="*" MinWidth="166" MaxWidth="500"/>
                            <DataGridTextColumn Header="Matricula" Binding="{Binding Matricula}" IsReadOnly="True" Width="*" MinWidth="150" MaxWidth="100"/>
                            <DataGridTextColumn Header="Cargo" Binding="{Binding Cargo}" IsReadOnly="True" Width="*" MinWidth="150" MaxWidth="200"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <StackPanel Grid.Row="2" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                        <Button Content="Adicionar Usuário" Click="AdicionarUsuario_Click" Width="200" Margin="10, 10, 10, 0" Style="{StaticResource EmphasisButtonStyle}" FontSize="18"/>
                        <Button Content="Editar Usuário" Click="EditarUsuario_Click" Width="150" Margin="10, 10, 10, 0" Style="{StaticResource EmphasisButtonStyle}" FontSize="18"/>
                        <Button Content="Deletar Usuário" Click="DeletarUsuario_Click" Width="150" Margin="10, 10, 10, 0" Style="{StaticResource CancelButtonStyle}" FontSize="18"/>
                    </WrapPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
