<Window x:Class="WMS_RadiadoresLemos_WPF.src.Views.ConfirmarSenhaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirmar Senha" Height="200" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        Loaded="Window_Loaded">

    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Por favor, insira sua senha para continuar:" Foreground="White" FontSize="16" Margin="0,0,0,10"/>
            <PasswordBox FontSize="16" Height="35" x:Name="SenhaPasswordBox" KeyDown="SenhaPasswordBox_KeyDown"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="Confirmar" Width="100" Margin="10,0" Click="Confirmar_Click" FontSize="16"/>
                <Button Content="Cancelar" Width="100" Margin="10,0" Click="Cancelar_Click" FontSize="16"
                        Style="{StaticResource CancelButtonStyle}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
