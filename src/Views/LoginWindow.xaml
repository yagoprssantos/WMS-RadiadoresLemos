<Window x:Class="WMS_RadiadoresLemos_WPF.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WMS_RadiadoresLemos_WPF"
        mc:Ignorable="d"
        Title="LoginWindow" Height="350" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        Loaded="Window_Loaded">

    <Grid>
        <StackPanel Width="300" VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- Campo de Texto para Usuário -->
            <TextBlock Foreground="{StaticResource TextBrush}" Text="Usuário" Margin="0,0,0,10" FontWeight="Bold" FontSize="16"/>
            <TextBox Name="UsernameField" Width="Auto" Height="30" Margin="0,0,0,20" FontSize="14" VerticalAlignment="Center" KeyDown="LoginField_KeyDown"/>

            <!-- Campo de Texto para Senha -->
            <TextBlock Foreground="{StaticResource TextBrush}" Text="Senha" Margin="0,0,0,10" FontWeight="Bold" FontSize="16"/>
            <PasswordBox Name="PasswordField" Width="Auto" Height="30" Margin="0,0,0,20" FontSize="14"
                         KeyDown="LoginField_KeyDown"/>

            <StackPanel Orientation="Horizontal" Width="300" HorizontalAlignment="Center" VerticalAlignment="Center">
                <!-- Botão para Confirmar -->
                <Button Content="Entrar" FontSize="16" Width="140" Height="40" Click="ConfirmarLogin_Click" Margin="2.5,0,10,0" Style="{StaticResource EmphasisButtonStyle}"/>

                <!-- Botão para Sair da aplicação -->
                <Button Content="Sair" FontSize="16" Width="140" Height="40" Click="Sair_Click" Style="{StaticResource CancelButtonStyle}"/>
            </StackPanel>

            <!-- Botão de Login Rápido para Admin (apenas para desenvolvimento) -->
            <Button Content="Login Rápido (Admin)" FontSize="16" Width="200" Height="40" Click="LoginRapidoAdmin_Click" 
                    Margin="0,20,0,0" Style="{StaticResource EmphasisButtonStyle}" Opacity="0.7"/>
        </StackPanel>

        <!-- Grid para escurecer o fundo e mostrar a barra de progresso -->
        <Grid x:Name="LoadingGrid" Visibility="Collapsed" Background="#80000000">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Carregando..." x:Name="TextoCarregamento" Foreground="{StaticResource TextBrush}" FontSize="16" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                <ProgressBar x:Name="LoadingProgressBar" 
                             IsIndeterminate="True" 
                             Width="300" 
                             Height="15"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
