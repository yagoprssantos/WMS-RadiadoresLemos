<UserControl x:Class="WMS_RadiadoresLemos_WPF.src.Views.DetalhesVendaUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Border Background="{StaticResource PanelBackgroundBrush}" CornerRadius="10" Padding="10" Margin="10" Width="650" Height="Auto" HorizontalAlignment="Center" VerticalAlignment="Top">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="550" Margin="10">
                <TextBlock Text="Produto 1" FontSize="30" FontWeight="Bold" Foreground="{StaticResource TextBrush}" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                <Separator Margin="0,0,0,20"/>
                <Border Background="{StaticResource SecondaryBrush}" CornerRadius="10" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Cliente: ID/Nome/null" Style="{StaticResource VendasTextBox}" />
                        <TextBlock Text="Data da compra: 1/01/2025" Style="{StaticResource VendasTextBox}" />
                        <TextBlock Text="Data do pagamento: 1/01/2025" Style="{StaticResource VendasTextBox}" />
                        <TextBlock Text="Preço: R$100,00" Style="{StaticResource VendasTextBox}" />
                        <TextBlock Text="ADICIONAR MAIS INFORMAÇÕES +" Style="{StaticResource VendasTextBox}" Margin="0,20,0,0"/>
                    </StackPanel>
                </Border>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="Imprimir PDF" Style="{StaticResource EmphasisButtonStyle}" Width="200" Margin="10"/>
                    <Button Content="Editar" Style="{StaticResource EmphasisButtonStyle}" Width="200" Margin="10"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
