<UserControl x:Class="WMS_RadiadoresLemos_WPF.DashboardUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Painéis para Contadores e Resumos -->
        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Stretch">

            <!-- Contadores (Usuários, Produtos, etc.) -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0, 20, 0, 20">
                <Border Background="#1976D2" Padding="20" CornerRadius="15" Margin="15">
                    <StackPanel>
                        <TextBlock Text="Total de Usuários" FontSize="18" Foreground="White" FontWeight="Bold"/>
                        <TextBlock x:Name="TotalUsuariosTextBlock" FontSize="26" Foreground="White" FontWeight="Bold" Text="0"/>
                    </StackPanel>
                </Border>
                <Border Background="#388E3C" Padding="20" CornerRadius="15" Margin="15">
                    <StackPanel>
                        <TextBlock Text="Total de Produtos" FontSize="18" Foreground="White" FontWeight="Bold"/>
                        <TextBlock x:Name="TotalProdutosTextBlock" FontSize="26" Foreground="White" FontWeight="Bold" Text="0"/>
                    </StackPanel>
                </Border>
                <Border Background="#FFA000" Padding="20" CornerRadius="15" Margin="15">
                    <StackPanel>
                        <TextBlock Text="Produtos com Baixo Estoque" FontSize="18" Foreground="White" FontWeight="Bold"/>
                        <TextBlock x:Name="ProdutosBaixoEstoqueTextBlock" FontSize="26" Foreground="White" FontWeight="Bold" Text="0"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Gráfico de Barras para Exemplo de Produtos Vendidos -->
            <GroupBox Header="Marcas com maior estoque" Margin="15" Foreground="White">
                <lvc:CartesianChart Series="{Binding EstoqueMarcasSeries}" LegendLocation="Right" Height="276">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Marcas" Labels="{Binding MarcasLabels}"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Quantidade em Estoque" LabelFormatter="{Binding FormatadorDeEixoY}"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </GroupBox>

            <!-- Logs Recentes -->
            <GroupBox Header="Últimos Eventos" Margin="15" Foreground="White">
                <ListBox x:Name="UltimosEventosListBox" Height="150" />
            </GroupBox>

        </StackPanel>
    </Grid>
</UserControl>
